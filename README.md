# Connect to remote Node

## Overview

This repository contains the code for connecting to a remote node and configure DIDComm service.

## Usage

### Prerequisites

Configure environment variables:

```bash
cp .env.example .env
```

### Run

Configure `configure_didcomm.js` with the function you want to run and run it:

```bash
node src/configure_didcomm.js
```
